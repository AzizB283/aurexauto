import type { Express } from "express";

export function registerRoutes(app: Express) {
  // No API routes - this is a static site
  // The contact form will be connected to an external service by the user
}
